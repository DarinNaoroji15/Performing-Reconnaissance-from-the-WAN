# Performing Reconnaissance from the WAN

In this lab, you will be performing reconnaissance from the Kali machine with an external IP address on the WAN (wide area network). Using nmap and John the Ripper, the attacker will leverage the stolen credentials to break into the Windows Server machine using remote desktop.

View Phases Here:

